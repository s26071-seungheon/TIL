# Service
 - 애플리케이션의 핵심 비즈니스 로직을 처리
 - Controler와 Repository 사이에서 업무처리

## Service의 특징
 - 책임 분리
    - 서비스는 단일 책임을 가진다(역할 명확 구분)
 - 업무처리담당 모든 코드가 모두 Service에 모인다
    - 입력값 검증, 여러 Repo 조합 처리, 트랜잭션 관리, 예외처리

## 서비스를 써야하는 이유
 - 유지보수가 쉬워진다
 - 코드 재사용성이 증가한다
 - 코드 깔끔해짐
 - 트랜잭션 처리에 최적이다