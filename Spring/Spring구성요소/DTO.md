# DTO(Data Transfer Object)
 - 계층 사이에서 데이터를 전달하기 위한 객체
 - Entity를 그대로 쓰지 않고 필요한 데이터만 담아 전달

 ## DTO를 쓰는 이유
 - 보안
   - 화면에 안보여주고 싶은 데이터가 있을 수 있음
 - 계층 분리
   - Entity는 DB 구조와 직결되어 있는데<br> Controller/화면과 직접 연결하면 계층구조가 깨짐
 - 데이터 가공
   - 화면에 필요한 형태로 변환이 가능
ㅇ