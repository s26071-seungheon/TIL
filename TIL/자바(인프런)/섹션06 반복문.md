# 34강
 ## 반복문
  #### 반복문은 코드를 반복해서 실행할 때 사용한다.

### 종류
#### while
#### do-while
#### for

# 35~36강
 ## while문
  #### while(조건식) { //코드 } 방식
  #### 조건식 확인하고 참이면 코드 블록 실행 거짓이면 벗어난다.
 ### 장점
  #### 1. 루프의 조건이 루프 내부에서 변경되는 경우, whie루프는 이를 관리하기 쉽다.
  #### 2. for루프보다 더 복잡한 조건과 시나리오에 적합하다.
  #### 3. 조건이 충족되는 동안 계속해서 루프를 실행하며, 종료 시점을 명확하게 알 수 없는 경우에 유용하다.
 ### 단점
  #### 1. 초기화, 조건체크, 반복 후의 작업이 분산되어 있어 코드를 이해하거나 작성하기 어려울 수 있다.
  #### 2. 루프 변수가 while 블록 바깥에서도 접근 가능하므로, 이 변수를 실수로 변경하는 상황이 발생 할 수 있다.

# 37강
 ## do while
  #### while문과 비슷하지만 조건에 상관없이 무조건 한 번은 코드를 실행한다.

# 38강
 ## break
  #### 반복문을 즉시 종료하고 나간다

 ## continue
  #### 반복문의 나머지 부분을 건너뛰고 다음 반복으로 진행하는데 사용한다.

# 39강
 ## for문
  #### for문도 while과 같은 반복문이고 주로 반복 횟수가 정해져 있을 때 사용한다.
  #### for(초기식; 조건식; 증감식)
 ### 장점
  #### 1. 초기화, 조건 체크, 반복 후의 작업을 한 줄에서 처리할 수 있어 편리하다.
  #### 2. 정해진 수만큼의 반복을 수행하는 경우에 사용하기 적합하다.
  #### 3. 루프 변수의 범위가 for 루프 블록에 제한되므로, 다른 곳에서 이 변수를 실수로 변경할 가능성이 적다.
 ### 단점
  #### 1. 초기화, 조건 체크, 반복 후의 작업이 분산되어 있어 코드를 이해하거나 작성하기 어려울 수 있다.
  #### 2. 루프 변수가 while 블록 바깥에서도 접근 가능하므로, 이 변수를 실수로 변경하는 상황이 발생할 수 있다.
  
  
 ### for문 실행 순서
  #### 1. 초기식이 실행된다. 주로 반복 횟수와 관련된 변수를 선언하고 초기화 할 때 사용한다. 초기식은 딱 1번 사용된다.
  #### 2. 조건식을 검증한다. 참이면 코드를 실행하고, 거짓이면 for문을 빠져나간다.
  #### 3. 코드를 실행한다.
  #### 4. 코드가 종료되면 증감식을 실행한다. 주로 초기식에 넣은 반복 횟수와 관련된 변수의 값을 증가할 때 사용한다.
  #### 5. 다시 2. 조건식부터 시작한다. (무한 반복)

# 40강
 ## for문2
  #### for문에서 초기식, 조건식, 증감식은 선택이다. 즉 생략해도 된다는 소리이다.<br> 단 생략해도 각 영역을 구분하는 세미콜론(;)은 유지를 해야한다.
  #### for(;;)는 while(true)와 같다.

# 41강
 ## 중첩 반복문
  #### 반복문은 내부에 또 반복문을 만들 수 있다. for, while 모두 가능하다.
